import { Notification } from "../../services/types"

interface ColleagueRequestProps {
  notification: Notification
}

export default function ColleagueRequest({notification}: ColleagueRequestProps) {
  return (
    
  )
}